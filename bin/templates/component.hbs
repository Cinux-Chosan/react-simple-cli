import React, { Component } from 'react'
{{#if connect}}
{{!-- 如果需要 redux connect，则引入 --}}
import { connect } from 'react-redux'
{{/if}}
{{#if isComponent}}
import styles from './style.less'
{{else}}
// import styles from './style.less'
{{/if}}

{{#if connect}}
export class {{capitalize (getPathName name)}} extends Component {
    constructor(props) {
        super(props)
    }

    render() {
        {{#if isComponent}}
        return (
            <div className={styles.root}>

            </div>
        )
        {{else}}
        return <>{this.props.yield}</>
        {{/if}}
    }
}

const mapStateToProps = () => {
    return {

    }
}

const mapDispatchToProps = dispatch => {
    return  {
        dispatch
    }
}

export default connect(mapStateToProps, mapDispatchToProps)({{capitalize (getPathName name)}})

{{else}}
export default class extends Component {
    constructor(props) {
        super(props)
    }

    render() {
        {{#if isComponent}}
        return (
            <div className={styles.root}>

            </div>
        )
        {{else}}
        return <>{this.props.yield}</>
        {{/if}}
    }
}
{{/if}}
